import React, { Component } from 'react';
import './styles.css';

export default class AddCard extends Component {
  render() {
    return (
      <div className="card addCard">AddCard</div>
    );
  }
}