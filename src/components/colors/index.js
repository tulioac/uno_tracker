import blueSvg from '../../svg/colors/blue.svg';
import redSvg from '../../svg/colors/red.svg';
import yellowSvg from '../../svg/colors/yellow.svg';
import greenSvg from '../../svg/colors/green.svg';

const blue = {
  name: "blue",
  svg: blueSvg
}

const red = {
  name: "red",
  svg: redSvg
}

const yellow = {
  name: "yellow",
  svg: yellowSvg
}

const green = {
  name: "green",
  svg: greenSvg
}

const colors = [blue, red, yellow, green];

export default colors;